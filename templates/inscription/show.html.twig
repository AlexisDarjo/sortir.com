{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Date</th>
                <td>{{ inscription.date ? inscription.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>



    {{ include('inscription/_delete_form.html.twig') }}
{% endblock %}
